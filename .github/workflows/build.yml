name: Build KT6PRO Stable

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialization
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libncurses5-dev gawk git gettext libssl-dev xsltproc rsync wget unzip zlib1g-dev python3
          
      - name: Clone Source
        run: |
          git clone -b v23.05.4 --depth 1 https://github.com/immortalwrt/immortalwrt.git openwrt
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Load Config
        run: |
          [ -f kt6pro.config ] && cp kt6pro.config openwrt/.config || echo "No config found"
          cd openwrt
          make defconfig

      - name: Build
        run: |
          cd openwrt
          make -j$(nproc) || make -j1 V=s

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: KT6PRO_Stable_Binaries
          path: openwrt/bin/targets/qualcommax/ipq50xx/
